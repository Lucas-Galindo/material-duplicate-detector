pandas>=2.2
openpyxl>=3.1
pytest>=8.0

# Interface web (deploy em servidor Linux, ver DEPLOY.md)
flask>=3.0
gunicorn>=21.2

# Empacotamento desktop (opcional, so necessario para gerar o .exe local)
pyinstaller>=6.0
